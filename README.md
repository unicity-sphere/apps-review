# Sphere SDK Testnet — Community App Review

Work-in-progress review of apps submitted to the Unicity Sphere SDK testnet call for builders.

- `index.html` — the review page (enable GitHub Pages in Settings to publish it publicly)
- `data/moderation_review_full.csv` — raw review data, one row per submission
